[project]
name = "whatsapp-mcp-server"
version = "0.1.0"
description = "Python MCP server for local WhatsApp chat search, read, send, and media operations via the Go bridge."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.9.0",
    "requests>=2.32.3",
]
